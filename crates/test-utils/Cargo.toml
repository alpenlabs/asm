[package]
name = "strata-asm-test-utils"
version = "0.1.0"
edition = "2024"

[dependencies]
strata-identifiers.workspace = true

arbitrary.workspace = true
bitcoin.workspace = true
rand_core = { workspace = true, features = ["getrandom"] }

[lints]
workspace = true
